const user = {
    firstName:'morioh',
    lastName:'Huncho',
    age:23

}
 console.table(user)
 console.log(user)

const userName = user.firstName.toUpperCase() + user.lastName.toUpperCase()
console.log('Welcome \t' + userName)